---
description: Audit and refine opencode's own config, agents, and commands
agent: config-tuner
---
Improve the opencode configuration toward this goal. Audit the relevant files
under ~/.config/opencode/, apply focused changes, validate any JSON, then run
`git -C ~/.dots diff` and summarize for review.

Goal: $ARGUMENTS
