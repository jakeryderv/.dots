# Commits that are pure formatting churn, skipped by `git blame`.
#
# GitHub honours this file automatically. Locally, enable it once with:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add a full 40-character SHA per line, newest last.

# shell: normalize formatting with shfmt (tabs -> 4 spaces, per .editorconfig)
d175a3293df172a6f3459fdd32621cd9fa5eca45

# lua: normalize formatting with stylua (parens per config/nvim/.stylua.toml,
# and wezterm.lua tabs -> 2 spaces per .editorconfig)
83af3f11ae9a2cc59f995d0910710cc2c789a5ab

# flake: re-indent the package list to match the genAttrs nesting
94875a11170242eaf0e18c2bd27b14ac318edef8
