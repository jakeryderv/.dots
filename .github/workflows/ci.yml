name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify-readmes:
    name: Every package dir has a README
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash _helpers/verify-readmes.sh
